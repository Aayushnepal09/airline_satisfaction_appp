# -----------------------------------------------------------------------------
# Local defaults (safe to commit for grading if you remove secrets)
# -----------------------------------------------------------------------------
APP_ENV=local

# FastAPI will load the model from this path inside the container
MODEL_PATH=/app/models/global_best_model_optuna.pkl

# Optional (used in notebooks/scripts if you want to read the SQLite database)
# For Render.com you can set this to a persistent disk path (e.g. /var/data/airline.db)
DATABASE_PATH=/app/data/airline.db

# Streamlit reads API_URL from docker-compose, but you can override it here if needed
# API_URL=http://api:8000
